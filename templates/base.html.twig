<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="http://localhost/task/public/build/app.css" rel="stylesheet"/>

        <script src="http://localhost/task/public/build/app.js" defer></script>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    {% include 'menu.html.twig' %}
    <body>
        {% if errors is defined and errors|length > 0 %}
        {% include 'messages/error.html.twig' %}
        {% endif %}

        {% if success is defined and success|length > 0 %}
        {% include 'messages/success.html.twig' %}
        {% endif %}
        
        {% block body %}{% endblock %}
    </body>
</html>
